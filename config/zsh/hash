#!/bin/zsh

local ANKI=~/Documents/Code/anki-decks
local APE=~/Documents/Code/ape-innovation
local CODE=~/Documents/Code
local DISK=~/Documents/Code/EDAA40
local DOC=~/Documents
local DOT=~/Documents/Code/dotfiles
local DOW=~/Downloads
local HBG=~/Documents/Code/elteamhbg
local INTROPROG=~/Documents/Code/introprog
local LTH=~/Documents/Skola/LTH
local NOTES=~/Documents/notes
local SECRET=~/Documents/Code/super-secret
local SKOLA=~/Documents/Skola
local STATS=~/Documents/Stats

test -d "$ANKI" && hash -d anki="$ANKI"
test -d "$APE" && hash -d ape="$APE"
test -d "$CODE" && hash -d code="$CODE"
test -d "$DOC" && hash -d doc="$DOC"
test -d "$DOT" && hash -d dot="$DOT"
test -d "$DOW" && hash -d dow="$DOW"
test -d "$HBG" && hash -d hbg="$HBG"
test -d "$INTROPROG" && hash -d introprog="$INTROPROG"
test -d "$LTH" && hash -d lth="$LTH"
test -d "$NOTES" && hash -d notes="$NOTES"
test -d "$SECRET" && hash -d secret="$SECRET"
test -d "$SKOLA" && hash -d skola="$SKOLA"
test -d "$STATS" && hash -d stats="$STATS"
