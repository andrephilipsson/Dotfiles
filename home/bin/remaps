#!/bin/sh

# Customizes the keyboard layout

# Use swedish keyboard layout
setxkbmap se -option shift:both_capslock -option terminate:ctrl_alt_backspace

# Increase the keyboard rate
xset r rate 300 100

# Enable numlock
numlockx
