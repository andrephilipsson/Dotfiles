lua <<
  CorpusDirectories = {
    ['~/Documents/notes'] = {
      autocommit = true,
      autoreference = 1,
      autotitle = 1,
      base = './',
      transform = 'local',
    }
  }
.

let g:CorpusPreviewWinhighlight='Normal:NonText'
